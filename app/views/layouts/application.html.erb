<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>MusicRails</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <br>
      <p>
        <% if user_signed_in? %>
          Bon jeu <strong><%= current_user.email %></strong> ! 
          <%= link_to 'Se déconnecter', destroy_user_session_path, method: :delete, class: 'btn btn-default' %>
        <% else %>
          <%= link_to 'Se connecter', new_user_session_path, class: 'btn btn-default' %>
          <%= link_to 'S\'inscrire', new_user_registration_path, class: 'btn btn-default' %>
        <% end %>
        <%= link_to 'Home', root_path, class: 'btn btn-default' %>
      </p>

      <%= yield %>
    </div>
  </body>
</html>
